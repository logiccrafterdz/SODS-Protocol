[workspace]
resolver = "2"
members = [
    "sods-core",
    "sods-verifier",
    "sods-p2p",
    "sods-cli",
    "sods-causal", "demo/defi-protocol",
]
# sods-zk requires RISC Zero toolchain; excluded from default workspace builds
exclude = ["sods-zk"]
